{
  "name": "me2em-protocol-monorepo",
  "version": "0.5.0-alpha.1",
  "private": false,
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "build-docs": "typedoc"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=9.0.0"
  },
  "devDependencies": {
    "typedoc": "^0.26.11",
    "typescript": "5.6"
  }
}